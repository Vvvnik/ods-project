include::../partials/attr_common.adoc[]
:doctype: book
:custom_title_page: none
:custom_other_page: none
= Архитектура и состав системы ODS
:sectnums:

Документ описывает архитектуру проекта ODS, структуру всех папок и файлов, а также логику работы системы генерации документации.

== Диаграмма архитектуры системы

[plantuml, architecture-diagram]
----
include::../partials/architecture.puml[plantuml, architecture-diagram]
----

== Корневая структура проекта

Корневая структура проекта:

* components/ – папка с компонентами документации;
* tools/ – инструменты и скрипты для генерации;
* resources/ – ресурсы (темы, шрифты);
* ui-files/ – пользовательский интерфейс для Antora;
* docker/ – Docker конфигурация и образы;
* templates/ – шаблоны и примеры;
* public/ – сгенерированный сайт документации (Antora);
* .gitlab-ci.yml – конфигурация CI/CD;
* antora-playbook.yml – конфигурация Antora;
* .gitignore – исключения для Git.

== Папка components/

Содержит все компоненты документации проекта.

=== Структура компонента

Каждый компонент имеет модульную структуру:

* modules/ – модули документации;
** ROOT/ – основной модуль;
*** pages/ – страницы документации (.adoc файлы);
*** partials/ – частичные файлы;
*** attachments/ – вложения (PDF файлы);
*** images/ – изображения;
*** examples/ – исходные файлы;
** ed/, trd/, oed/ – дополнительные модули.

Детальная структура ROOT модуля:

* nav.adoc – навигация по модулю;
* pages/ – страницы документации (.adoc файлы);
* partials/ – частичные файлы;
** lists/ – автоматически генерируемые списки;
** docs-api/ – документация API (если включена);
** docs-db/ – документация БД (если включена);
* attachments/ – вложения (PDF файлы);
* images/ – изображения;
** bpmn/ – BPMN диаграммы (если включены);
** drawio/ – Draw.io диаграммы (если включены);
** images-db/ – схемы БД (если включены);
* examples/ – исходные файлы;
** bpmn/ – исходники BPMN диаграмм;
** drawio/ – исходники Draw.io диаграмм;
** sources.yml – источники API;
** database.yml – профили БД.

=== Компоненты проекта

Компоненты проекта:

* airport-service – сервис аэропорта;
* data-processing-engine – движок обработки данных;
* information-system – информационная система;
* toys-service – сервис игрушек;
* project-guide – руководство по проекту.

== Папка docker/

Содержит Docker конфигурацию и скрипты сборки образов.

=== Структура

* Dockerfile.universal – универсальный образ с Ruby + Node.js + все зависимости;
* docker-compose.yml – конфигурация для локальной разработки;
* build-images.sh – скрипт сборки универсального образа;
* build-kroki.sh – скрипт сборки Kroki образа;
* README.md – документация по Docker.

=== Назначение

* Контейнеризация всех инструментов генерации;
* Упрощение развертывания и разработки;
* Готовые образы для CI/CD.

== Папка templates/

Содержит шаблоны и примеры для документации.

=== Структура

* diagrams/ – шаблоны диаграмм (PlantUML);
* glossary/ – шаблон глоссария;
* snippets/ – сниппеты кода для редакторов;
* templates_doc/ – шаблоны документов.

== Папка public/

Сгенерированный сайт документации (создается Antora).

=== Структура

* _/ – статические ресурсы (CSS, JS, изображения);
* {component}/ – страницы каждого компонента;
* index.html – главная страница;
* search-index.js – индекс для поиска.

== Папка tools/

Содержит все инструменты для генерации документации.

=== Основной скрипт

* start.rb – главный скрипт, координирующий всю генерацию.

=== Скрипты генерации (scripts/)

* convert_ascii_to_pdf.rb – универсальный конвертер PDF;
* bpmn_to_img.js – конвертер BPMN диаграмм в SVG;
* drawio_to_img.js – конвертер Draw.io диаграмм в SVG;
* db_to_adoc.rb – конвертер БД в AsciiDoc;
* openapi-swagger-adoc.rb – конвертер API в AsciiDoc;
* generate_lists.rb – генератор списков файлов;
* translations_one.rb – переводы;
* viewer.html – просмотрщик;

=== Библиотеки (lib/)

* list_generator.rb –––енератор списков файлов;
* pdf_generator.rb –––енератор PDF;
* approval_page_generator.rb –––енератор листов утверждения;
* config_loader.rb –––агрузчик конфигурации;
* config_parser.rb –––арсер конфигурации;
* title_page_blocks.rb –––локи титульных страниц;
* ordinal_word_ru.rb –––орядковые числительные на русском;
* utils.rb –––спомогательные функции;

=== Конвертеры

* convert_title_page_original.rb –––тандартная титульная страница;
* convert_title_page_contract.rb –––итульная страница для контрактов;
* convert_title_page_original_frame.rb –––итульная страница с рамкой;
* convert_approval_page_gost.rb –––ист утверждения ГОСТ;
* convert_other_page_original.rb –––бычные страницы (стандартные);
* convert_other_page_original_frame.rb –––бычные страницы (с рамкой);
* convert_list_alphabetic.rb –––онвертер списков (alphabetic);
* convert_list_dash.rb –––онвертер списков (dash);

=== Конфигурация

* config.yml ––основная конфигурация проекта;
* config_type_page.yml ––конфигурация типов страниц и списков;

== Папка resources/

Содержит ресурсы для оформления документов.

=== Темы

* themes/ ––папка с темами PDF;
** report-theme.yml ––основная тема отчетов;
** frame-theme.yml ––тема для документов с рамками;
** act-theme.yml ––тема для актов;
** base-theme.yml ––базовая тема;
** default-theme.yml ––тема по умолчанию;

=== Шрифты

* fonts/ ––папка со шрифтами (если используется);

== Папка ui-files/

Содержит пользовательский интерфейс для Antora сайта.

=== Структура

* ui-bundle.zip ––основной UI bundle (архив с темами Antora);
* ui-bundle-style/ ––дополнительные файлы стилей для кастомизации;
** css/ ––кастомные CSS стили;
** font/ ––дополнительные шрифты;
** img/ ––изображения для интерфейса;
** js/ ––JavaScript файлы;
** layouts/ ––шаблоны макетов;
** partials/ ––частичные шаблоны.

=== Назначение

Папка `ui-files/` используется для:
* Настройки внешнего вида сайта документации;
* Переопределения стандартных стилей Antora;
* Добавления кастомных элементов интерфейса;
* Настройки цветовой схемы и типографики.

Конфигурация UI происходит через секцию `ui` в `antora-playbook.yml`:
* `bundle.url` – ссылка на основной UI bundle;
* `supplemental_files` – путь к дополнительным файлам стилей.

== Файлы конфигурации

=== CI/CD

* .gitlab-ci.yml – конфигурация GitLab CI/CD;
** Содержит этапы сборки PDF и сайта;
** Настройки переменных окружения;
** Правила запуска пайплайнов.

=== Antora

* antora-playbook.yml – конфигурация Antora;
** Настройки сборки сайта документации;
** Конфигурация Kroki сервера;
** Атрибуты для Antora;
** UI bundle и supplemental_files для кастомных стилей.

=== Git

* .gitignore – исключения для Git;
** Игнорирует временные файлы;
** Исключает сгенерированные изображения;
** Сохраняет необходимые скрипты.

== Логика работы start.rb

=== Основные этапы

* Чтение конфигурации из config.yml;
* Обработка каждого компонента;
* Генерация диаграмм (BPMN, Draw.io);
* Генерация API документации;
* Генерация БД документации;
* Генерация PDF документов;
* Генерация списков файлов.

=== Условная логика

* Проверка переменной ENV['CI'];
** В CI отключается генерация диаграмм;
** В CI отключается генерация API;
** В CI отключается генерация БД;
* Локально работает полная генерация.

== Конфигурация config.yml

=== Секция defaults

Содержит настройки по умолчанию для всех компонентов.

* bpmn – настройки BPMN диаграмм;
** enabled – включить/отключить генерацию;
** src – исходная папка;
** dst – папка назначения;
** erase_destination_folder – очистка папки;

* drawio – настройки Draw.io диаграмм;
** enabled – включить/отключить генерацию;
** src – исходная папка;
** dst – папка назначения;
** erase_destination_folder – очистка папки;

* api – настройки API документации;
** enabled – включить/отключить генерацию;
** dst – папка назначения;
** api_sources_file – файл источников;
** erase_destination_folder – очистка папки;

* db – настройки БД документации;
** enabled – включить/отключить генерацию;
** images_dir – папка изображений;
** dst – папка назначения;
** database_connect_file – файл подключения;
** profile – профиль БД;

* pdf – настройки PDF генерации;
** enabled – включить/отключить генерацию;
** src – исходная папка;
** dst – папка назначения;
** converter – путь к конвертеру;
** theme – тема оформления;
** use_title_pages – титульные страницы;

* list – настройки генерации списков;
** enabled – включить/отключить генерацию;
** adoc_lists – списки .adoc файлов;
** pdf_lists – списки PDF файлов;
** dst – папка назначения.

=== Секция components

Содержит список компонентов с индивидуальными настройками.

* Каждый компонент наследует настройки из defaults;
* Можно переопределить любые параметры;
* Порядок секций не важен.

== Модульная архитектура конвертеров

=== Универсальный конвертер

* convert_ascii_to_pdf.rb – основной конвертер;
** Загружает конфигурацию из config_type_page.yml;
** Динамически подключает модули;
** Выбирает тему PDF автоматически.

=== Модули титульных страниц

* convert_title_page_original.rb – стандартная титульная;
* convert_title_page_contract.rb – для контрактов;
* convert_title_page_original_frame.rb – с рамками;
* convert_approval_page_gost.rb – лист утверждения ГОСТ.

=== Модули обычных страниц

* convert_other_page_original.rb – с полными рамками;
* convert_other_page_original_frame.rb – с рамками и приставкой.

=== Модули списков

* convert_list_alphabetic.rb – стиль alphabetic;
* convert_list_dash.rb – стиль dash.

== Автоматическая генерация

=== Списки файлов

* Генерируются автоматически;
* Создаются в modules/ROOT/partials/lists/;
* Обновляются при каждой сборке;
* Содержат навигацию по документам.

=== PDF документы

* Генерируются из .adoc файлов;
* Сохраняются в modules/ROOT/attachments/;
* Используют настроенные темы;
* Поддерживают титульные страницы.

=== Диаграммы

* BPMN конвертируются в SVG через Puppeteer;
* Draw.io конвертируются в SVG через Desktop приложение;
* PlantUML обрабатываются через Kroki;
* Сохраняются в соответствующих папках images/.

== Потоки данных

=== Генерация диаграмм

* Исходники в examples/ → скрипты конвертации → изображения в images/.

=== Генерация документации

* Исходники БД/API → скрипты конвертации → частичные файлы в partials/.

=== Генерация PDF

* AsciiDoc файлы в pages/ → универсальный конвертер → PDF файлы в attachments/.

=== Генерация списков

* Сканирование файлов → генератор списков → частичные файлы в partials/lists/.

== Цветовая схема диаграммы

* Синий – файлы конфигурации;
* Фиолетовый – скрипты генерации;
* Зеленый – библиотеки и модули;
* Оранжевый – ресурсы (темы, шрифты);
* Розовый – компоненты документации.
