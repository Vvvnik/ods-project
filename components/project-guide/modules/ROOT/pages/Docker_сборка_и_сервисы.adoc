include::../partials/attr_common.adoc[]
:doctype: book
:custom_title_page: none
:custom_other_page: none
:sectnums:

= Docker: сборка и сервисы

== Образы

* **Универсальный образ**: registry.gitlab.com/vvvnik/ods-project/ruby-node-antora:2:
** Содержит Ruby 3.2 + Node.js 20 + Asciidoctor + Antora + все необходимые пакеты;
** Используется в CI для сборки PDF и сайта;
** Собирается из `docker/Dockerfile.universal`;
** Все версии пакетов зафиксированы для воспроизводимости сборок (gems и npm пакеты с точными версиями).
** В том числе глобально устанавливаются Antora CLI, генераторы сайта, `+@antora/lunr-extension+` и `+@opendevise/antora-swagger-ui-extension+` — они должны соответствовать списку в секции `antora.extensions` файла `antora-playbook.yml`.
* **Kroki сервис**: registry.gitlab.com/vvvnik/ods-project/kroki:0.28.0:
** Отдельный контейнер для рендеринга диаграмм;
** Подключается как сервис в CI;
** Базовый образ: yuzutech/kroki:0.28.0.
* **Ollama сервис**: registry.gitlab.com/vvvnik/ods-project/ollama:1 (опционально):
** Отдельный контейнер для LLM переводов;
** Подключается как сервис в CI или docker-compose;
** Базовый образ: ollama/ollama:latest;
** Модель: gemma:latest (предзагружена в образе с моделью).

== Скрипты сборки
=== Сборка универсального образа

[source,bash]
----
# Сборка и отправка в реестр (по умолчанию)
./docker/build-images.sh [tag]

# Только сборка без отправки в реестр
./docker/build-images.sh [tag] --no-push

# Примеры
./docker/build-images.sh 2
./docker/build-images.sh 2 --no-push
----

=== Сборка Kroki образа

[source,bash]
----
# Сборка и отправка в реестр (по умолчанию)
./docker/build-kroki.sh [tag]

# Только сборка без отправки в реестр
./docker/build-kroki.sh [tag] --no-push

# Примеры
./docker/build-kroki.sh 0.29.0
./docker/build-kroki.sh 0.29.0 --no-push
----

[[kroki-local-port]]
==== Запуск Kroki с пробросом порта (локально)

Скрипт `build-kroki.sh` только **собирает** (и при необходимости **пушит**) образ; **порт на хост** задаётся отдельно при `docker run`.
Без **`-p 8000:8000`** контейнер не слушает хост, и `host.docker.internal:8000` из сервисов `site` / `site-preview` в `docker-compose` не достучится до Kroki.

После `./docker/build-kroki.sh --no-push` (локальный тег по умолчанию `0.28.0`) или после `docker pull` того же образа, что публикуется в реестр для CI:

[source,bash]
----
docker rm -f ods-kroki 2>/dev/null; docker run -d --name ods-kroki -p 8000:8000 registry.gitlab.com/vvvnik/ods-project/kroki:0.28.0
----

Проверка: **http://localhost:8000**. В **GitLab CI** этот же образ подключается как сервис **`kroki:8000`** — схема в пайплайне не меняется, меняется только способ запуска на машине разработчика.

=== Сборка Ollama образа
==== Базовый образ (без модели)

Модель загружается при запуске контейнера:

[source,bash]
----
# Сборка и отправка в реестр (по умолчанию)
./docker/build-ollama.sh [tag]

# Только сборка без отправки в реестр
./docker/build-ollama.sh [tag] --no-push

# Примеры
./docker/build-ollama.sh 1
./docker/build-ollama.sh 1 --no-push
----

==== Образ с предзагруженной моделью

Модель загружается во время сборки образа (размер образа 10+ GB):

[source,bash]
----
# Сборка и отправка в реестр (по умолчанию)
./docker/build-ollama-with-model.sh [tag] [model]

# Только сборка без отправки в реестр
./docker/build-ollama-with-model.sh [tag] [model] --no-push

# Примеры
./docker/build-ollama-with-model.sh 1 gemma:latest
./docker/build-ollama-with-model.sh 1 gemma:latest --no-push
----

⚠️ **Важно**: Образ с моделью очень большой (10+ GB), загрузка в реестр может занять много времени.

== Локальная разработка

=== Архитектура для локальной разработки

Для локальной разработки в `docker-compose.yml` достаточно **двух сервисов**:

* **`start`** - запуск `tools/start.rb` (генерация PDF и вспомогательных шагов);
* **`site`** - сборка сайта с Antora (генерация статики в `public/`).

Опционально доступен сервис предпросмотра:

* **`site-preview`** - сборка сайта и запуск HTTP-сервера на порту `8080` для просмотра в браузере.

Все внешние сервисы (Kroki, Ollama, PostgreSQL) работают **на хосте** и доступны контейнерам через `host.docker.internal`:

* **Kroki** - запускается на хосте, доступен через `host.docker.internal:8000` (настроено в `antora-playbook.yml`);
* **Ollama** - запускается на хосте, доступен через `host.docker.internal:11434` (настроено в `docker-compose.yml` через `OLLAMA_HOST`);
* **PostgreSQL** (pg-demo) - работает на хосте, доступен через `host.docker.internal` (настроено через `DB_HOST`).

Такая архитектура позволяет:

* Использовать минимальное количество контейнеров (только `pdf` и `site`);
* Переиспользовать сервисы, запущенные на хосте;
* Легко переключаться между локальной разработкой и CI/CD.

=== Использование готовых образов

[source,bash]
----
# Генерация PDF
docker-compose -f docker/docker-compose.yml run --rm start

# Сборка сайта документации
docker-compose -f docker/docker-compose.yml run --rm site

# Предпросмотр сайта (порт 8080)
docker-compose -f docker/docker-compose.yml up site-preview
#
# Альтернатива через run (нужно пробросить порты):
docker-compose -f docker/docker-compose.yml run --rm --service-ports site-preview
#
# Откройте ссылку из вывода сервера (Ctrl+Click).
# Для выхода из режима сервера: Ctrl+C.

# С конкретным компонентом
docker-compose -f docker/docker-compose.yml run --rm start ruby tools/start.rb airport-service
----

:note-caption: Важно

[NOTE]
После Docker сборки сайта Antora выводит сообщение `Open file:///app/public/index.html`, 
но файл нужно открывать в `public/index.html` проекта локальной сборки (на хосте), 
так как директория `/app` внутри контейнера монтируется в корень проекта.

Если при использовании `site-preview` в логах `http-server` появляются 404 запросы на `/_/css/~@fontsource/...`, это обычно связано с кешированием в браузере/сервере.
Перезапустите `site-preview` и сделайте hard refresh страницы (Cmd+Shift+R / Ctrl+F5).

=== Проверка подключения к сервисам

Перед запуском убедитесь, что все необходимые сервисы запущены на хосте:

[source,bash]
----
# Проверка Kroki
# Проверяем Kroki рендером PlantUML (это то, что реально нужно проекту)
printf '%s\n' '@startuml' 'Alice -> Bob: test' '@enduml' | \
  curl -sS -X POST http://localhost:8000/plantuml/svg \
    -H 'Content-Type: text/plain' \
    --data-binary @- -o /dev/null -w 'HTTP=%{http_code} CT=%{content_type}\n'
# Ожидаемый ответ:
# HTTP=200 CT=image/svg+xml

# Проверка Ollama
# Ollama опциональна — проверяйте только если используете LLM
curl -sS http://localhost:11434/api/version || true
# Ожидаемый ответ (пример):
# {"version":"0.13.4"}

# Проверка PostgreSQL
# psql может быть не установлен на хосте — проверим, что порт доступен
nc -vz localhost 5432
# Ожидаемый ответ (пример):
# Connection to localhost port 5432 [tcp/postgresql] succeeded!
----

=== Настройка сервисов для docker-compose
==== Настройка Kroki

Сейчас `kroki-server-url` передаётся при запуске Antora через CLI-атрибут:

[source,bash]
----
antora generate --attribute kroki-server-url=$KROKI_SERVER_URL antora-playbook.yml
----

Поэтому `antora-playbook.yml` **не нужно править** под разные окружения.

* Локально (docker-compose) `KROKI_SERVER_URL` по умолчанию: `http://host.docker.internal:8000`
* В CI `KROKI_SERVER_URL`: `http://kroki:8000`

===== Диаграммы (PlantUML) и почему они могут “пропадать”

В проекте диаграммы PlantUML рендерятся через расширение `asciidoctor-kroki`, то есть нужен доступный сервер Kroki.

Важно различать два режима:

* *Рендер “на этапе сборки” (встроенные SVG/PNG в `public/`)*:
Antora обращается к Kroki во время генерации и кладёт картинки в `public/**/_images/`.
В этом режиме диаграммы будут отображаться при просмотре HTML даже без запущенного Kroki.

* *Без рендера “на этапе сборки”*:
Antora собирает страницы, но не запрашивает диаграммы у Kroki во время генерации.
В этом режиме диаграммы могут:
** не появиться в HTML;
** появляться/исчезать при пересборке, если `public/` перезаписывается;
** “казаться” видимыми из-за старых файлов в `public/`, если вы не делали `--clean`.

В `docker/docker-compose.yml` для сервиса `site` используется `kroki-fetch-diagram!`, поэтому Kroki *не требуется для сборки*, но диаграммы *не встраиваются* в результат сборки.

Если вам нужно, чтобы `docker-compose -f docker/docker-compose.yml run --rm site` всегда генерировал встроенные диаграммы:

* уберите `--attribute kroki-fetch-diagram!` из команды сервиса `site`;
* убедитесь, что Kroki доступен по `KROKI_SERVER_URL` во время сборки.

==== Настройка Ollama

По умолчанию в `docker/docker-compose.yml` для сервиса `start` уже настроен `OLLAMA_HOST` на Ollama, запущенную на хосте.
Менять нужно только если вы запускаете Ollama в Docker/CI и хотите ходить на `http://ollama:11434`.

[source,yaml]
----
# docker/docker-compose.yml
services:
  start:
    environment:
      # Для docker-compose локальной разработки (Ollama на хосте):
      - OLLAMA_HOST=http://host.docker.internal:11434
      
      # Для CI/CD (Ollama как сервис в Docker):
      # - OLLAMA_HOST=http://ollama:11434
----

Адрес Ollama также настраивается в `tools/config.yml` через переменную окружения:

[source,yaml]
----
# tools/config.yml
ollama:
  enabled: true
  host: "${OLLAMA_HOST:-http://localhost:11434}"  # Настраивается через OLLAMA_HOST
----

**Варианты настройки:**

* **Локальная разработка** (Ollama на хосте): +
  `OLLAMA_HOST=http://localhost:11434` или не указывать (значение по умолчанию)

* **Docker Compose** (Ollama на хосте, контейнеры подключаются): +
  `OLLAMA_HOST=http://host.docker.internal:11434`

* **CI/CD** (Ollama как сервис в Docker):
  `OLLAMA_HOST=http://ollama:11434`

:note-caption: Важно

[NOTE]
Перед отправкой изменений в CI убедитесь, что в `docker-compose.yml` установлено `OLLAMA_HOST=http://ollama:11434`, иначе сборка в CI не сможет подключиться к Ollama сервису.

== Структура папки docker

[source,text]
----
docker/
├── Dockerfile.universal          # Универсальный образ
├── docker-compose.yml            # Локальная разработка
├── build-images.sh              # Сборка универсального образа
├── build-kroki.sh               # Сборка Kroki образа
├── build-ollama.sh              # Сборка Ollama образа (без модели)
├── build-ollama-with-model.sh   # Сборка Ollama образа (с моделью)
└── README.md                    # Документация по Docker
----

== Архитектура

Проект использует **универсальный подход** с одним основным образом и дополнительными сервисами:

* **ruby-node-antora:2** – основной образ для всех операций сборки;
* **kroki:0.28.0** – сервис для диаграмм (PlantUML, Mermaid, Graphviz);
* **ollama:1** – сервис для LLM переводов (опционально);

Это обеспечивает:

* Быструю сборку (один основной образ);
* Надежность (все зависимости в одном месте);
* Простоту поддержки (модульная архитектура);
* Гибкость (сервисы можно включать/отключать по необходимости);

include::../partials/docker-compose-snippets.adoc[]