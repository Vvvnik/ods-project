# Ключи задач Jira, которые выгружаются как «основные»:
# для каждой создаётся папка tools/jira/<KEY>/ с KEY.md и подзадачами.
# Одна строка – один ключ. Пустые строки и строки с # в начале игнорируются.
# Порядок строк задаёт порядок секций в tools/jira/description.adoc (см. python/config.yaml issue_order_file).

ODS-6
ODS-10
