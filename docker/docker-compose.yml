version: '3.8'

services:
  # Генерация PDF документов
  pdf:
    image: registry.gitlab.com/vvvnik/my-project/ruby-node-antora:1
    container_name: my-project-pdf
    volumes:
      - .:/app
    working_dir: /app
    command: ruby tools/start.rb

  # Сборка сайта с Antora
  site:
    image: registry.gitlab.com/vvvnik/my-project/ruby-node-antora:1
    container_name: my-project-site
    volumes:
      - .:/app
    working_dir: /app
    command: antora antora-playbook.yml
